# ===================================
# BACKEND ENVIRONMENT VARIABLES
# ===================================
# Copy this file to .env and update with your values

# Server Configuration
NODE_ENV=development
PORT=5000

# Session Secret (REQUIRED)
# Generate with: openssl rand -base64 32
# IMPORTANT: Use a strong random string in production!
SESSION_SECRET=your-super-secret-key-change-this-min-32-characters

# Frontend URL for CORS (Required for separate deployments)
# For local development with separate frontend
FRONTEND_URL=http://localhost:5173

# For production (Vercel frontend), example:
# FRONTEND_URL=https://your-app.vercel.app

# Multiple origins (comma-separated), example:
# FRONTEND_URL=https://your-app.vercel.app,https://staging.vercel.app

# ===================================
# DATABASE (Optional - Future use)
# ===================================
# PostgreSQL Database URL
# DATABASE_URL=postgresql://username:password@host:port/database

# ===================================
# EMAIL (Optional - Future use)
# ===================================
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
