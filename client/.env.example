# ===================================
# FRONTEND ENVIRONMENT VARIABLES
# ===================================
# Copy this file to .env.local and update with your values
# All frontend variables MUST start with VITE_ prefix

# API Backend URL
# For local development (running backend separately)
VITE_API_URL=http://localhost:5000

# For production (Render backend), example:
# VITE_API_URL=https://your-backend.onrender.com

# For production (Railway backend), example:
# VITE_API_URL=https://your-backend.up.railway.app

# ===================================
# NOTES
# ===================================
# - Leave VITE_API_URL empty if frontend and backend run together (same port)
# - For separate deployments, set this to your backend URL
# - Always include the protocol (http:// or https://)
# - Do NOT include trailing slash
