{
  "version": 2,
  "buildCommand": "cd client && npm install && NODE_ENV=production npm run build",
  "outputDirectory": "client/dist",
  "installCommand": "echo 'Skipping root install'",
  "framework": "vite"
}
